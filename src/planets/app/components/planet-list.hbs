<div class="planetList">
  <h2>Planet list</h2>
  {{#each this.planets as |planet index|}}
  <LinkTo @route="index" @query={{hash id=index}} class="h-12 flex items-center border-white cursor-pointer no-underline">
    {{planet}} 
  </LinkTo>
  {{/each}}
</div>
